services:
  - type: web
    name: twin-bot
    env: python
    pythonVersion: "3.12"   # ← ye line force karegi 3.12
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT